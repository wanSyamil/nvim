-- ~/.config/nvim/lua/plugins/visual-multi.lua
return {
  "mg979/vim-visual-multi",
  lazy = false, -- Required: Must be loaded immediately
  init = function()
    -- Optional: Configure keybindings here
  end,
}
